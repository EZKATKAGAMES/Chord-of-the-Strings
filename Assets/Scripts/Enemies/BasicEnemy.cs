using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicEnemy : MonoBehaviour
{

    // Base class for non boss enemy types
    // This script only contains stat information to apply onto enemies.

    public int health;
    int movespeed;
    int attackPower;

    
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
